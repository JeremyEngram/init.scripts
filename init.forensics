#!/bin/bash

 sudo apt install -y forensics-all; 
 sudo apt install -y forensics-all-gui; 
 sudo apt apt install -y forensics-colorize; 
 sudo apt install -y forensics-extra; 
 sudo apt install -y forensics-extra-gui; 
 sudo apt install -y forensics-full
