#!/bin/bash
#AutoSnort
echo "Autosnort - Scripts For Interfacing With Snort Using Automated Scripts"; echo 

sudo git clone https://github.com/JeremyEngram/snort.git; sudo chmod +x /usr/local/bin/snort/*; sudo /usr/local/bin/snort/init.snort

cd /opt; git clone https://github.com/da667/Autosnort.git; cd Autosnort
cat README*
cd Autosnort-Ubuntu; sudo chmod +x *.sh
ls

